# Console — https://guiders-console.ancoradual.com

server {
    listen 80;
    listen [::]:80;
    server_name guiders-console.ancoradual.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    root /var/www/guiders-frontend/current/console;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
